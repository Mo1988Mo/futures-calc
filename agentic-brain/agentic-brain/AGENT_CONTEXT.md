# AGENT CONTEXT
If you open this project fresh, here's what to know:

- Main bot: bot.py — run with `python bot.py`
- Docs folder: docs/ — contains 3 company knowledge files
- Calculator: futures-trading-tool.jsx — React component
- API key needed: set ANTHROPIC_API_KEY in environment
- All answers are grounded in docs/ only — no hallucination
